// A PWM Motor
//
// It can rotate the dish on Az or Alt
//
// O7

namespace Drive;

attribute "priority";

enum State : byte { Stopped, Forward, Reverse, Stalled }

table PWMMotor {
  state:State = Stopped;
  position:uint16;
}

root_type PWMMotor;